<div class="col-md-6">
    <h3><%= @brands.first.name %></h3>
    <div class="row">
        <div class="col-md-6">
            <h5><%= @categories.first.name %></h5>
            <% @products.where(brand_id: 1, category_id: 1).each do |f| %>
            <%= link_to f.name, product_path(f) %>
            <% end %>
        </div>
        <div class="col-md-6">
            <h5><%= @categories.second.name %></h5>
            <% @products.where(brand_id: 1, category_id: 2).each do |f| %>
            <%= link_to f.name, product_path(f) %>
            <% end %>
        </div>
    </div>

</div>
<div class="col-md-6">
    <h3><%= @brands.second.name %></h3>
    <div class="row">
        <div class="col-md-6">
            <h5><%= @categories.first.name %></h5>
            <% @products.where(brand_id: 2, category_id: 1).each do |f| %>
            <%= link_to f.name, product_path(f) %>
            <% end %>
        </div>
        <div class="col-md-6">
            <h5><%= @categories.second.name %></h5>
            <% @products.where(brand_id: 2, category_id: 2).each do |f| %>
            <%= link_to f.name, product_path(f) %>
            <% end %>
        </div>
</div>

